apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-service
spec:
  template:
    spec:
      containers:
      - name: app
        env:
        - name: REDIS_NODES
          # 关键：用逗号分隔多个节点，Spring 会自动拆分
          value: "192.168.1.1:6379,192.168.1.2:6379,192.168.1.3:6379"
