apiVersion: kustomize.config.k8s.io/v1
kind: Kustomization

resources:
  - redis-deployment.yaml
  - redis-svc.yaml

secretGenerator:
  - name: redis-secret
    literals:
      - redis_password=redis123
