management:
  endpoints:
    web:
      exposure:
        include: "health,info,prometheus,loggers"
spring:
  flyway:
    locations: classpath:db/migration
  cloud:
    kubernetes:
      config:
        enabled: true